# Axela Configuration
# Copy this file to .env and fill in the values

# Data directory for persistent files (SQLite DB, etc.)
# Default: /data (for Docker), or current directory for local dev
# AXELA_DATA_DIR=/data

# Database
# If not set, SQLite is used in DATA_DIR/axela.db
# For PostgreSQL:
# AXELA_DATABASE_URL=postgresql+asyncpg://axela:axela@localhost:5432/axela
# For Docker Compose with PostgreSQL (set in docker-compose.yml):
# AXELA_DATABASE_URL=postgresql+asyncpg://axela:axela@postgres:5432/axela

# Telegram Bot
# Get from @BotFather on Telegram
AXELA_TELEGRAM_BOT_TOKEN=your_bot_token_here

# Note: chat_id is stored in database, set via API:
# PUT /api/v1/settings/telegram_chat_id {"value": 123456789}
# Or send /start to the bot to see your chat ID

# Encryption key for storing credentials
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
AXELA_ENCRYPTION_KEY=your_fernet_key_here

# AWS Bedrock AI Summarization (optional)
# Set AXELA_BEDROCK_ENABLED=true to enable AI summaries per project
# Requires AWS credentials (via env vars, IAM role, or ~/.aws/credentials)
# AXELA_BEDROCK_ENABLED=true
# AXELA_BEDROCK_REGION=us-east-1
# AXELA_BEDROCK_MODEL_ID=amazon.nova-lite-v1:0

# Scheduling
AXELA_DEFAULT_TIMEZONE=Europe/Lisbon
AXELA_MORNING_DIGEST_TIME=08:00
AXELA_EVENING_DIGEST_TIME=19:00

# Collector settings
AXELA_INITIAL_FETCH_DAYS=7

# API settings
AXELA_API_HOST=0.0.0.0
AXELA_API_PORT=8000

# Logging
AXELA_LOG_LEVEL=INFO
AXELA_LOG_JSON=false
